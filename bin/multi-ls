#!/bin/sh
# List contents of multiple directories with labeled output.
# Usage: multi-ls dir1 dir2 dir3 ...

if [ $# -eq 0 ]; then
  echo "Usage: multi-ls dir1 [dir2 ...]" >&2
  exit 1
fi

for target in "$@"; do
  echo "==> $target <=="
  ls "$target" 2>&1
  echo "---"
done
