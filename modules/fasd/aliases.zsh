#
# Aliases
#

alias j='fasd_cd -id'                    # Changes the current working directory interactively.
alias v='fasd -f -t -e $EDITOR'             # use Vim's builtin list of accessed files (similar rupa/v)
# alias v='fasd -f -e vim'                 #
# alias v='fasd -f -t -e vim -b viminfo'   # use Vim's builtin list of accessed files (similar rupa/v)
